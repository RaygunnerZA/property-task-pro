import Dashboard from "@/app/page";

/** Records / compliance workbench — formerly the Records tab on Home. */
export default function RecordsPage() {
  return <Dashboard workbenchPanel="records" />;
}
