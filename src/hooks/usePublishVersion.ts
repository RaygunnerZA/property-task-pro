export function usePublishVersion() {
  const publish = (reviewId: string) => {
    // Placeholder: will call ruleBuilder.publishVersion()
    console.log('Publishing version for review:', reviewId);
  };

  return { publish };
}
