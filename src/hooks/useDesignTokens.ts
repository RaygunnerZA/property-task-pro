/**
 * Re-export useDesignTokens from provider for convenience
 */
export { useDesignTokens, type DesignTokens, type ColorTokens, type ShadowTokens, type ButtonTokens, type ChipTokens, type SegmentTokens, type CalendarTokens, defaultTokens } from '@/providers/DesignTokenProvider';
