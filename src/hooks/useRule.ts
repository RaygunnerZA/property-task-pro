export const useRule = (ruleId: string) => {
  // Placeholder: will call complianceRules.getRule()
  return { 
    data: { 
      id: ruleId, 
      title: 'Example Rule', 
      description: '' 
    }, 
    loading: false 
  };
};
