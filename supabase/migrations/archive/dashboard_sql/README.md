# Archived Dashboard SQL

These files were pasted in the SQL Editor and **diverged** from migrations (e.g. `apply_views_migration.sql` omitted owner/contact columns that the matching migration included).

They are not applied by `db reset`. Do not run them against production.
